# Server configuration
PORT=5000                # The port where the backend server will run

# Application mode
NODE_ENV=development    # Options: development, production, test

# OpenAI API configuration
# To enable AI-powered analysis, obtain an API key from https://platform.openai.com/
# Leave empty to use the fallback scoring method (no AI)
OPENAI_API_KEY=

# Optional: Customize the AI model (if using OpenAI)
# OPENAI_MODEL=gpt-3.5-turbo  # Default model

# Optional: File upload settings
# MAX_UPLOAD_SIZE=10        # Maximum file size in MB
# UPLOAD_DIRECTORY=uploads  # Directory for temporary file storage